{{ docker_daemon_options | combine(
    {'registry-mirrors': docker_registry_mirrors} if docker_registry_mirrors | length > 0 else {},
    {'insecure-registries': docker_insecure_registries} if docker_insecure_registries | length > 0 else {}
  ) | to_nice_json }}
